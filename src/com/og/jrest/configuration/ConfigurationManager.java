package com.og.jrest.configuration;

public class ConfigurationManager {

	public static String getControllerPackageNames() {
		// TODO Read this from the api.config file
		return null;
	}

	/*
	 * TODO Implement a configuration manager. Should be able to load configuration
	 * settings from a file called api.config. The methods in here should probably
	 * be static.
	 * 
	 * Should allow access to the controller package name for now. This will do
	 * many, many things eventually
	 */

}
